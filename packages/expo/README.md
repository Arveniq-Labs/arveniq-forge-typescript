# Forge Expo

Use this adapter with `expo-secure-store` (passed as an adapter rather than bundled) to retain short-lived mobile-gateway credentials in platform secure storage. Forge Developer API keys are never accepted.
