# Forge React Native

React Native adapter for `@arveniq/forge-mobile-core`. Pass an app-owned secure token store and lifecycle adapter; this package does not ship a Developer API client or a plaintext credential store.
